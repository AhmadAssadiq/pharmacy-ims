{
  "name": "pharmacy-ims",
  "version": "1.0.0",
  "private": true,
  "description": "Pharmacy Inventory Management System - graduation project (PSUT). React client, Node.js API, Python/scikit-learn forecasting service.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "db:migrate": "npm run db:migrate -w server",
    "db:seed": "npm run db:seed -w server",
    "test": "npm test -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
